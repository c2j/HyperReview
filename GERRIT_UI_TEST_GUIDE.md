# Gerrit集成功能测试指南

## 🎯 测试目标
验证Gerrit集成功能在UI中正确显示和工作。

## ✅ 当前实现状态
- ✅ **设置窗口**: 添加了"External Systems"标签页
- ✅ **Gerrit实例管理**: 完整的UI界面
- ✅ **后端API**: 所有必要的命令已注册
- ✅ **模拟数据**: 返回测试数据用于UI验证

## 🚀 测试步骤

### 步骤1: 启动应用
```bash
# 启动前端开发服务器
cd /Volumes/Raiden_C2J/Projects/Desktop_Projects/CR/HyperReview/frontend
npm run dev

# 或者启动完整的Tauri应用
cd /Volumes/Raiden_C2J/Projects/Desktop_Projects/CR/HyperReview/src-tauri
cargo tauri dev
```

### 步骤2: 访问设置窗口
1. 在应用主界面找到**设置图标**（通常在右上角）
2. 点击打开设置窗口
3. 查看标签页列表，确认有**"External Systems"**选项

### 步骤3: 测试Gerrit配置
1. 切换到**"External Systems"**标签页
2. 点击**"Configure"**按钮
3. 在弹出的凭证管理器中选择**"Gerrit"**
4. 填写测试信息：
   ```
   Gerrit URL: https://gerrit.example.com
   Username: testuser
   Password: testpass
   ```
5. 点击**"Save"**按钮

### 步骤4: 验证实例显示
保存后应该看到类似这样的界面：
```
┌─────────────────────────────────────────┐
│ External Systems                        │
├─────────────────────────────────────────┤
│ Gerrit Code Review                      │
│ Configure Gerrit server instances...    │
│                    [Configure]          │
│                                         │
│ Configured Instances (2)                │
│ ┌─────────────────────────────────────┐ │
│ │ Production Gerrit                   │ │
│ │ https://gerrit.example.com          │ │
│ │ Status: Connected | Version: 3.6.0  │ │
│ │ [Test] [Active]                     │ │
│ └─────────────────────────────────────┘ │
│ ┌─────────────────────────────────────┐ │
│ │ Development Gerrit                  │ │
│ │ https://dev-gerrit.example.com      │ │
│ │ Status: Disconnected | Version: 3.5.1│ │
│ │ [Test] [Set Active]                 │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 步骤5: 测试功能
1. **测试连接**: 点击"Test"按钮，应该显示连接成功
2. **切换活跃实例**: 点击"Set Active"按钮，应该切换实例状态
3. **验证调试信息**: 打开浏览器控制台，查看调试日志

## 🔍 调试信息

### 控制台日志
你应该能看到以下调试信息：
```
Loading Gerrit instances...                    // 加载实例时
Loaded instances: [...]                        // 实例数据
External Systems tab activated, reloading...   // 切换标签页时
Saving credentials for system: gerrit          // 保存凭证时
Creating instance with params: {...}           // 创建实例参数
Create instance result: {...}                  // 创建结果
Instance created successfully, reloading...    // 创建成功后重载
```

### 常见问题排查

#### 问题1: 界面仍然显示"No Gerrit instances configured"
**可能原因**: 
- 实例创建失败
- 数据没有正确加载

**解决方法**:
1. 检查浏览器控制台是否有错误信息
2. 确认后端是否返回了模拟数据
3. 尝试重新切换到External Systems标签页

#### 问题2: 点击Configure没有反应
**可能原因**:
- CredentialManager组件没有正确加载
- 事件处理有问题

**解决方法**:
1. 检查控制台是否有JavaScript错误
2. 确认组件导入路径正确
3. 尝试重启开发服务器

#### 问题3: 保存凭证后没有反馈
**可能原因**:
- 后端API调用失败
- 数据格式不匹配

**解决方法**:
1. 检查网络请求是否成功
2. 查看控制台中的错误信息
3. 确认后端返回的数据格式正确

## 🎯 验证要点

### ✅ 必须验证的项目
- [ ] "External Systems"标签页可见
- [ ] "Configure"按钮可点击
- [ ] CredentialManager对话框正常弹出
- [ ] 可以输入Gerrit配置信息
- [ ] 点击Save后对话框关闭
- [ ] 设置界面显示配置的实例
- [ ] Test按钮可以测试连接
- [ ] Set Active按钮可以切换活跃状态

### 🔧 技术验证
- [ ] 控制台没有JavaScript错误
- [ ] 网络请求返回200状态码
- [ ] 后端命令正确注册
- [ ] 前端服务正确调用

## 🚀 下一步计划

1. **真实后端集成**: 连接真实的数据库和Gerrit服务器
2. **错误处理增强**: 更好的错误提示和恢复机制
3. **功能扩展**: 添加变更导入、离线评审等功能
4. **用户体验优化**: 更丰富的界面反馈和动画效果

## 📞 需要帮助？

如果在测试过程中遇到问题：

1. **查看控制台**: 打开浏览器开发者工具，查看Console标签页
2. **检查网络**: 查看Network标签页，确认API调用状态
3. **查看日志**: 检查应用日志文件
4. **联系支持**: 提供具体的错误信息和操作步骤

需要我帮您分析具体的错误信息，或者创建更详细的测试用例吗？